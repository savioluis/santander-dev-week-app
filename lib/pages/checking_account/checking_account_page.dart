import 'package:flutter/material.dart';

class CheckingAccountPage extends StatelessWidget {
  const CheckingAccountPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Checking Account Page"),
      ),
    );
  }
}